import { FETCH_GROCERIES, NEW_GROCERY, REMOVE_GROCERY, TOGGLE_GROCERY } from "../actions/types";

const initialState = {

};

export default function(state=initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
